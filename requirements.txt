0# ===========================================
# 无人机配送路径规划 - 依赖清单
# ===========================================
# 核心依赖（运行所有求解器）
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
scikit-learn>=1.0.0

# 可选 - 运行探索性数据分析(EDA)时需要
seaborn>=0.12.0
scipy>=1.9.0

# ===========================================
# 说明：
# 1. 运行贪心/遗传/模拟退火等算法只需安装核心依赖。
# 2. 仅当执行 c101_eda.py 时才需要 seaborn 和 scipy。
# 3. 若希望使用 Jupyter 交互式分析，可自行添加 jupyter、ipython。
# ===========================================
